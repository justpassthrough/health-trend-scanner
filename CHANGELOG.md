# Health Trend Scanner — 변경 이력

## 프로젝트 정보
- **GitHub**: `justpassthrough/health-trend-scanner`
- **로컬 경로**: `C:\Users\Andy\OneDrive\Desktop\Fire\de_novo\Claude_code_output\health-trend-scanner`
- **대시보드**: https://justpassthrough.github.io/health-trend-scanner/
- **자동 실행**: GitHub Actions, 하루 4회 (07:30/13:00/19:00/00:00 KST)

## 점수 공식
```
최종점수 = H(주제온도) × I(검색의도) × P(약사근거) × G(전문가갭) × Y(유튜브)
```
- H: 네이버 DataLab 7일 평균 변화율 기반 (0~100)
- I: 의심/경고형 vs 구매/추천형 비율 (0.5~1.5)
- P: 약사 전문 키워드 매칭 (0.7~1.3)
- G: 일반 블로그 vs "약사" 블로그 비율 (0.7~1.3)
- Y: 유튜브 48시간 내 급등 영상 (1.0~1.3)

## 2026-06-13 대개편 — 2트랙 + 검색광고 + 시의형 신선도 정렬

**배경**: 글감이 일부(바나바잎·시서스추출물·리포좀멀티비타민 등 짧은 성분명)만 쓸모있고 나머지는 쓸모없게 느껴짐.
- **고장1**: AI가 "GLP-1 계열 비만치료제 복용 중 탈모 위험" 같은 *문장/앵글*을 keyword로 뱉음 → 검색·DataLab·뉴스 보강 전부 0 (14개 중 10개가 데이터 0).
- **고장2**: 제약 산업/주식/희귀질환 신약 뉴스가 영양제 글감과 같은 잣대로 섞여 상위에 뜸.
- **근본원인**: 절대 검색량(월 실검색수)을 몰라 "감마오리자놀(뉴스많음)"과 "AI의료기기(검색0)"를 구별 못 함.

**해결 (collect_v2.py / build_dashboard_v2.py / scan_v2.yml)**:
1. **검색광고 키워드도구 API 이식**(키워드 딥다이브에서): 월간 절대검색수 + 경쟁정도. `fetch_search_volume`, `calc_opportunity`, `opportunity_label`.
2. **2트랙 구조** — AI가 글감을 `track`으로 분류, 트랙마다 다른 잣대:
   - **검색형**(에버그린·성분/증상): 기회점수 = 약사가치 × 수요배수(log10 검색량) × 경쟁여유. **실검색량순** 정렬.
   - **시의형**(산업·정책·신약 뉴스): **신선도/신규성 우선** 정렬. 산업뉴스 안 버림 — 검색량 대신 속보성으로 평가.
   - 기존 4개(영양제·성분/약업계·정책/질환·치료/소비자건강)는 **보조 태그**로 유지.
3. **keyword/앵글 분리**: keyword는 짧은 검색어(2~12자)로만, 긴 설명은 why_now/angle/title_idea로. → 보강 0 해소.
4. **약사가치** = AI `pharma_value`(1~5) × 전문가갭배수(0.7~1.3).

**시의형 신선도 정렬 (핵심 — 이 툴의 목적은 '지금 막 뜨는 새 주제')**:
- 처음엔 시의형을 *뉴스량*으로 정렬 → 탈모건보처럼 크고 오래된 이슈가 1위로 오는 역효과 발견(사용자 지적).
- **수정**: `get_news_recency()` 신규 — 네이버 뉴스 발행일(pubDate, sort=date)로 "최신 기사 몇 시간 전 + 24h 기사 다발" 측정.
- 시의점수 = 약사가치 × **신선도배수(최신기사<24h=1.6 … >1주=0.5, 24h다발 가산)** × **신규성배수(이미작성=0.4, 첫등장=1.15)** × 급등(보조) × 뉴스규모(약한 보조). 뉴스량은 일부러 약하게.
- 신선도는 consecutive_days가 필요해서 enrich가 아니라 main에서 확정.
- 적용 범위: **시의형에만**(검색형은 기회점수 유지 — 바나바잎 같은 알짜 틈새가 밀리지 않게). 사용자 결정.

**대시보드**: 🔍검색형 / 📰시의형 두 섹션. 검색형 카드 `월검색│경쟁│변화│전문가갭`+💎황금배지, 시의형 카드 `최신기사│24h내│뉴스│전문가갭`+시의점수배지. 구버전 스캔(track 없음)은 전부 검색형으로 안전 폴백.

**필요 작업(사용자)**: GitHub Secrets에 `NAVER_AD_CUSTOMER_ID`(4413485)/`NAVER_AD_API_KEY`/`NAVER_AD_SECRET_KEY` 3개 등록(키워드 딥다이브와 동일 값). 없어도 동작하나 검색형 기회점수가 약사가치로 폴백.

**검증(실 API, AI 호출은 토큰 절약 위해 제외)**: 검색형 — 루테인(월4만)·바나바잎(월9.7천) 상단, 감마오리자놀(월520·경쟁高) 하단 교정. 시의형 — 위고비품절(신규·기사20h전) 1위, 탈모건보(이미작성·오래됨) 3위로 강등, 메디톡스리콜(기사30일전) 최하. AI 실제 순위는 GitHub Actions 첫 실행에서 확인.

## 2026-03-03 변경사항

### 1. AI 해석 기능 추가
- **파일**: `scripts/collect.py`, `scripts/build_dashboard.py`, `requirements.txt`, `.github/workflows/daily_scan.yml`
- **내용**: Claude Haiku 4.5로 상위 15개 키워드에 블로그 기획 포인트 자동 생성
- **프롬프트 설계**: 블로그 핵심 공식 "다들 ~라고 알잖아요. 근데 그거, 좀 달라요" 기반
  - 1줄: 뒤집기 — 대중 오해 vs 실제
  - 2줄: 앵글 — 병원 약사/DDS 연구자 차별화 소재
- **비용**: 1회 ~$0.005, 월 ~$0.60
- **Secrets**: `ANTHROPIC_API_KEY` (GitHub Secrets에 등록 완료)
- **Fallback**: API 키 없거나 실패 시 빈 문자열 (기존 기능 영향 없음)
- **대시보드 표시**: 연보라색 배경 (#1a1525), 💡 아이콘, 13px

### 2. 키워드 유지 로직
- **문제**: 매 스캔마다 뉴스에서 키워드를 새로 추출하므로, 이전 고점수 키워드가 사라질 수 있었음
- **해결**: 이전 스캔에서 점수 15+ 키워드를 이월
  - 새 키워드: 최대 20개 (뉴스 빈도순)
  - 이월 키워드: 최대 10개 (이전 점수 높은 순)
  - 총 30개 캡 유지 → API 호출량 변화 없음
- **건강 필터**: 이월 시에도 건강 관련 키워드만 (연예인 이름 자동 탈락)

### 3. 중복 키워드 병합
- **문제**: "비만치료제"/"비만약"/"마운자로" 등 같은 이슈가 여러 슬롯 차지
- **병합 규칙** (점수 높은 쪽이 대표):
  1. 포함 관계: "백신" ⊂ "코로나 백신" (짧은 쪽 3글자 이상)
  2. 띄어쓰기 제거 후 동일: "비만치료제" == "비만 치료제"
  3. 뉴스 헤드라인 2개 동일: 같은 이슈
- **대시보드 표시**: 대표 키워드 아래 "(= 비만약, 마운자로)" 형태

### 4. 건강 관련성 필터
- **문제**: "바이오하자드 레퀴엠", "산다라박" 등 건강 무관 키워드가 진입
- **해결 1**: 키워드 추출 시 `HEALTH_CONTEXT_WORDS`와 동시 출현 확인
- **해결 2**: 연예인 이름 불용어 직접 등록 (홍현희, 박봄, 산다라박 등)
- **결과**: 연예인/게임/스포츠 키워드 완전 제거

### 5. G 점수 로깅 강화
- `score_keyword()`에서 G 계산 상세 출력: 전체 건수, 전문가 건수, gap_ratio, 산식

## 파일 구조 (주요)
```
scripts/
├── collect.py          # 데이터 수집 + 점수 산출 + AI 해석
└── build_dashboard.py  # HTML 대시보드 생성
data/
├── latest.json         # 최신 스캔 결과
└── history/            # 히스토리 누적 (YYYY-MM-DD_HHMM.json)
docs/
└── index.html          # GitHub Pages 대시보드
.github/workflows/
└── daily_scan.yml      # 자동 실행 워크플로우
```

## 알려진 한계
- 키워드 유지는 latest.json 기반이므로, 한 번 빠진 키워드는 다음 뉴스 추출까지 복구 불가
- 연예인 불용어는 수동 관리 필요 (새 인물 등장 시 추가)
- "바이오" ⊂ "프로바이오틱스" 오병합 → 최소 3글자 조건으로 완화했으나, 에지 케이스 가능

## 블로그 글 작성 시스템 (참고)
- 핵심 공식: "다들 ~라고 알잖아요. 근데 그거, 좀 달라요"
- 필자 정체성: 병원 약사 + 바이오/나노메디신 연구자
- 제목: 심리 공식 5가지 (손실회피/권위/미완성/숫자/타겟팅)
- 본문: 도입(3단계 후킹) → 소제목 2~4개 → 마무리(CTA 3요소)
- 톤: 해요체, 짧은 문장, 쉬운 비유 필수
